directive : "o-checkbox"

inheritedAttributes: [{
  component: "FormDataComponent",
  path: "components/input/overview",
  attributes: ["attr", "label", "tooltip", "tooltipPosition", "tooltipShowDelay", "automatic-binding", "automatic-registering", "data", "enabled", "read-only"]
}]

outputs: [{
name : "onChange",
description: "Event triggered when component value changes"
}]
