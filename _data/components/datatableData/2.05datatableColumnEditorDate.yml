directive: "o-datatable-cell-editor-date"
title: "Cell editor: date"

attributes: [{
  name: "date-model-type",
  type: "timestamp | string",
  default: "timestamp",
  required: "",
  description: "If a column of type date is editable its model must be defined for saving its value. For example, in Ontimize classic server, dates have timestamp format (number) but to save them is necessary  to send a string with format <em>YYYY-MM-DD HH:MM:SS</em>. In this case, the <em>date-model-type</em> attribute should have <em>string</em> value, and <em>date-model-format</em> should be <em>YYYY-MM-DD HH:mm:ss</em> (see [MomentJS](http://momentjs.com/){:target='_blank'})"
},{
  name: "date-model-format",
  type: "string",
  default: "ISO date format",
  required: "",
  description: "Cover if the attribute <em>date-model-type</em> value is <em>string</em>"
}]
